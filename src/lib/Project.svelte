<article>
    <svelte:element this={"h" + hLevel}>{ p.title }</svelte:element>
    <img src={p.image} alt="">
    <p>{p.description}</p>
</article>

<script>
    export let info = {};
    let p = info;
    export let hLevel = 2;
</script>
